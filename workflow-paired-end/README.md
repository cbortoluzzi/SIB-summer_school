The exact snakemake command line to run the workflow will depend on the type of cluster and memory available. A general command would be:

```
snakemake --configfile config.yaml --snakefile Snakefile --cores 10
```

For more information about snakemake, run: 

```
snakemake --help
```
